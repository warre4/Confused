vertex shaders should have the extension ".vert"
fragment shaders should have the extension ".frag"

no other shaders will be exported

note: any .spv files in this directory will be automatically deleted
note: manualCompile.bat has a hardcoded OutDir location so it may break if anything changes